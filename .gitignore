# Secrets — never commit
.env
.env.*
.envrc
*.key
*.pem
keystore/
secrets/
.private/

# Foundry artifacts (broadcast/ contains tx data with deployer addresses; review before publishing)
contracts/cache/
contracts/out/
contracts/broadcast/
contracts/.gas-snapshot

# Hardhat (if used)
artifacts/
cache_hardhat/

# Anchor / Solana (if used)
.anchor/
target/
test-ledger/

# Node / Next
node_modules/
.next/
dist/
.turbo/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Local deployment artifacts that may carry operator addresses
deployments/local-*.json

# Internal planning / working notes — never published. Public docs go in docs/.
ai/
.private/

# build-prompt scaffolding (local only)
Goal.md
prompt.md
plan.md

# Ralph build scaffolding (local only, same class as Goal.md/prompt.md/plan.md)
.ralph/

# Internal agent instructions (contain operator-specific and personal detail)
CLAUDE.md
AGENTS.md

# Demo rehearsal working state (local only)
.demo-rehearsal-state.json

# Demo video working files (large, local only)
.video/
